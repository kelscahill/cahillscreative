{% if heading.level and heading.text %}
  {% set base_class = heading.base_class|default('o-heading') %}

  <h{{ heading.level }} class="{{ heading.type and heading.size ? base_class ~ ' ' : '' }}{{ bem_classes(base_class, heading.type, heading.size, heading.extra) }}">
    {% if heading.link.url %}
      <a class="{{ bem_classes(base_class, 'link') }}" href="{{ heading.link.url }}" {{ add_attributes(heading.link.attributes) }}>
        {{ heading.text }}
      </a>
    {% else %}
      {{ heading.text }}
    {% endif %}
  </h{{ heading.level }}>
{% endif %}
