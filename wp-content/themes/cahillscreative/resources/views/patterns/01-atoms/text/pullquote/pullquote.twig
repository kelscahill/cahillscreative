{% if pullquote %}
  {% set base_class = pullquote.base_class|default('o-pullquote') %}

  <pullquote class="{{ bem_classes(base_class, pullquote.element, pullquote.modifiers, pullquote.extra) }}" >
    {% if pullquote.text %}
      {{ pullquote.text }}
    {% endif %}
    {% if pullquote.cite %}
      <cite>{{ pullquote.cite }}</cite>
    {% endif %}
  </pullquote>
{% endif %}
