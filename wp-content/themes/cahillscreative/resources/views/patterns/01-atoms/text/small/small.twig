{% if small.text %}
  {% set small_base_class = small.base_class|default('o-small') %}

  <small class="{{ bem_classes(small_base_class, small.element, small.modifiers, small.extra) }}">
    {{ small.text }}
  </small>
{% endif %}
