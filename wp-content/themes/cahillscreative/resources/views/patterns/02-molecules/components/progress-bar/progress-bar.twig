{% set base_class = progress_bar.base_class|default('c-progress-bar') %}

{% if progress_bar %}
  <div class="{{ bem_classes(base_class) }} js-progress-bar">
    <progress value="{{ progress_bar.value ? progress_bar.value : '0' }}" max="100"></progress>
  </div>
{% endif %}
