{% if image.src %}
  {% set base_class = image.base_class|default('o-image') %}

  <img class="{{ bem_classes(base_class, image.element, image.modifiers, image.extra) }}{{ image.lazy == false ? ' no-lazy' }}"
    {% if image.lazy != false %}
      loading="lazy"
    {% endif %}
    {% if image.srcset %}
      srcset="{{ image.srcset }}"
    {% endif %}
    {% if image.sizes %}
      sizes="{{ image.sizes }}"
    {% endif %}
    src="{{ image.src }}"
    alt="{{ image.alt ? image.alt : "Image" }}"
    {% if image.title %}
      title="{{ image.title }}"
    {% endif %}
    {% if image.width %}
      width="{{ image.width }}"
    {% endif %}
    {% if image.height %}
      height="{{ image.height }}"
    {% endif %}
    data-pin-media="{{ image.src|towebp|resize(1200, auto) }}"
    {% if post.link or post.affiliate_link %}
      data-pin-url="{{ post.affiliate_link ? post.affiliate_link : post.link }}"
    {% endif %}
    {% if yoast_meta_description %}
      data-pin-description="{{ yoast_meta_description }}"
    {% endif %}
  />
{% endif %}
